{
  "name": "user",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest ",
    "userUnitTests": "jest --testPathPattern=/user/unit/ --watchAll --detectOpenHandles --runInBand --verbose",
    "userIntegrationTests": "jest --testPathPattern=/user/integration/ --watchAll --detectOpenHandles --runInBand --verbose",
    "userCoverage": "jest --coverage --testPathPattern=/user/ --verbose",
    "coverage": "jest --coverage --runInBand --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
        "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"

  },
  "jest": {
    "testPathIgnorePatterns": [
      "./user/integration/local-config.js",
      "./user/integration/config.js",
      "./local-database/*",
      "./node_modules/*"
    ]
  }
}
